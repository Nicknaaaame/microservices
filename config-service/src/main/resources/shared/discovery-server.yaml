spring:
  security:
    user:
      name: ${app.discovery.user}
      password: ${app.discovery.pass}

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false

server:
  port: 8761
